destination d_F5_bigip_gtm_dns_response_irule_syslog_chained {
    file("`log_path`/F5_bigip_gtm_dns_response_irule_syslog_chained/${HOST}/${YEAR}-${MONTH}-${DAY}-${HOUR}.log"
        template(t_date_orig_host_msghdrmsgonly)
    );
};
