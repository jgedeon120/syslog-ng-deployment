destination d_cisco_sourcefire_defensecenter_syslog {
    file("`log_path`/cisco_sourcefire_defensecenter_syslog/${HOST}/${YEAR}-${MONTH}-${DAY}-${HOUR}.log"
        template(t_date_host_msghdrmsgonly)
    );
};
